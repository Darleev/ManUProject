name: test-pipe-fail

on: [push, pull_request]
    
jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v2
    - name: test
      shell: bash
      run: make -C runtest test_seed
